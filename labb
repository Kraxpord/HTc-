#include <iostream> 
#include <chrono> 
#include <thread> 
#include <time.h> 
#include <Windows.h> 
using namespace std;
int main()
{
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    int columes, row, x, y, m, e, d, a, b, c, num, max;
    int operation;
    cout << "1-ex" << endl;
    cout << "2-ex" << endl;
    cout << "3-ex" << endl;
    cout << "4-ex" << endl;
    cout << "5-ex" << endl;
    cout << "6-ex" << endl;
    cout << "Enter operation ->>";
    cin >> operation;
    switch (operation)
    {
    case 1:
    {
    }break;
    case 2:
    {
    }break;
    case 3:
    {
        int main(); 
        {
        int num, max = INT_MIN;
        std::cout << "Enter 7 integers: ";
        for (int i = 0; i < 7; i++) {
            std::cin >> num;
            if (num > max) {
                max = num;
            }
        }
        std::cout << "The maximum number is: " << max << std::endl;
    }

    }break;
    case 4:
    {
    }break;
    case 5:
    {
    }break;
    case 6:
    {
    }break;

    default:
        break;
    }

    return 0;
}
